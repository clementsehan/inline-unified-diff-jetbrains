<!--
    Loaded only when the Kotlin plugin (org.jetbrains.kotlin) is present.
    Required by the verifier: optional <depends> must reference a config-file.
    K2 mode compatibility is declared in the main plugin.xml so the verifier
    can detect it without traversing optional config files.
-->
<idea-plugin/>
